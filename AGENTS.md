Project is for a formula 1 data display and analysis

The backend is supposed to be a simple typescript API, that read from other datasource, do small transformation and aggregation and expose to the app via REST API (it should cache the query by sessionkey)
Located on the /backend folder

The frontend is a React app that load data from backend (no auth between user/front and front/back) and display the data
Located on the /frontend folder
