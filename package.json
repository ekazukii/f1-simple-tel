{
  "name": "f1stuff-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "backend:dev": "dotnet watch --project backend/backend.csproj run",
    "backend:build": "dotnet build backend/backend.csproj",
    "frontend:dev": "npm --workspace frontend run dev",
    "frontend:build": "npm --workspace frontend run build"
  },
  "devDependencies": {
    "@types/three": "^0.182.0"
  }
}
